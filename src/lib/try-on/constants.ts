/** Maximum working edge length for try-on canvas (memory safety). */
export const TRYON_MAX_EDGE = 2048

/** Default bundled tattoo overlay for technical proof. */
export const TRYON_DEFAULT_TATTOO_SRC = '/images/designs/design-03.webp'

/** Accepted MIME types for photo upload. */
export const TRYON_ACCEPTED_TYPES = ['image/jpeg', 'image/png', 'image/webp']

/** Max upload file size in bytes (10 MB). */
export const TRYON_MAX_FILE_BYTES = 10 * 1024 * 1024
